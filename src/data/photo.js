/**
 * The portrait, in one place. The sidebar, the lightbox, the schema.org
 * ImageObject and the image sitemap all describe the same file, and search
 * engines compare the declared dimensions against the real ones, so replacing
 * public/armemon.webp means updating these numbers here and nowhere else.
 */
export const PHOTO = {
  src: '/armemon.webp',
  alt: 'Ahmed Raza Memon',
  width: 1000,
  height: 1250,
  caption: 'Ahmed Raza Memon, product developer, known online as armemon.',
}
